extend type Query {
  pagedOutstationAttendanceByKarkun(
    karkunId: String!
    queryString: String
  ): PagedAttendanceType

  outstationAttendanceByMonth(
    month: String!
    cityId: String
    cityMehfilId: String
  ): [AttendanceType]
}