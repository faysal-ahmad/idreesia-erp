type Attachment {
  _id: String
  name: String
  mimeType: String
  data: String
}

extend type Query {
  attachmentsById(ids: [String]!): [Attachment]
}

extend type Mutation {
  createAttachment(name: String, mimeType: String, data: String!): Attachment
  removeAttachment(_id: String!): Int
}
